# うちかえる 改善実装計画書

対象ファイル: `index.html`（HTML / CSS / JS 同梱の 1 ファイル構成）
作成日: 2026-06-19
進め方: Opus 単体で複数の専門役割を分担し、操作テスト → 課題抽出 → 役割間の意見擦り合わせ → 最適案選定 → 実装計画 → 実装、の順で進める。

---

## 1. 役割分担（すべて Opus が担当）

| 記号 | 役割 | 主担当領域 |
| --- | --- | --- |
| 🎨 AD | アートディレクター | ビジュアルデザイン・色使い・世界観の一貫性 |
| 🧭 UX | UX/UI デザイナー | 情報設計・操作性・画面遷移・コントロール |
| ⚙️ FE | フロントエンドエンジニア | 読み込み処理・描画性能・コードの安全性 |
| 🔍 QA | QA テスター | 操作テスト・不具合/使いづらさの発見 |
| ♿ UID | UID/アクセシビリティ | タッチ人間工学・モバイル統合・情報伝達 |

> 注記: 本環境はリモートのヘッドレス実行環境であり、実機ブラウザでの目視プレイはできない。
> そのため本テストは「コードのフロー追跡＋ヒューリスティック評価による疑似操作テスト」として実施し、
> 各画面・各処理の挙動をコードから再現・検証した結果に基づいて課題を洗い出している。

---

## 2. 操作テスト（疑似プレイスルー）

HOME → ルール → 名前入力 → カウントダウン → 初回武器選択 → プレイ → 波クリア後カード選択 → 結果、の全導線をコードから追跡。

| # | 手順 | 期待 | 観察結果 |
| --- | --- | --- | --- |
| T1 | HOME 表示 | メニュー表示・横スクロールなし | OK。ただし背景 Canvas が非表示状態でも毎フレーム描画されている（後述 I4） |
| T2 | ルール → 名前入力 | 遷移できる | OK |
| T3 | 名前未入力で開始 | 開始不可・フォーカス | OK（`nameStartBtn` で空欄ガード） |
| T4 | 3・2・1 カウントダウン | 開始合図が分かる | 弱い。ステータス帯の小さな文字のみで、画面中央に合図が出ない（I2） |
| T5 | 初回武器選択 | 3 枚から 1 つ選ぶ | OK。ただしカードのカテゴリが文字情報のみで瞬時に判別しづらい（I5） |
| T6 | プレイ（自動攻撃） | 敵を撃退できる | OK。防衛ライン（赤線）が alpha .25 と薄く、危険域が認識しづらい（I3） |
| T7 | 被弾・拠点 HP 減少 | 危機が伝わる | 弱い。HUD の「拠点 N」の数値が変わるだけで、瀕死時の警告演出がない（I6） |
| T8 | プレイ中に中断したい | 一時停止したい | 不可。リタイア（＝ゲーム終了）しか手段がなく、純粋な中断ができない（I1b） |
| T9 | 音を消したい | ミュートしたい | 不可。ビープ音のオン/オフ手段が一切ない（I1a） |
| T10 | 波クリア → カード選択 | 強化を選ぶ | OK。敵・弾・スコアは停止する（仕様どおり） |
| T11 | 拠点突破 → 結果 | 結果とランキング | OK。`○波クリア / ○○点` 形式 |
| T12 | iPhone SE 幅 | 横スクロールなし | OK |

---

## 3. 課題・改善点の洗い出し

| ID | 分類 | 課題 | 提起した役割 |
| --- | --- | --- | --- |
| I1a | 操作性/UX | 音のミュート手段がない | 🧭UX 🔍QA ♿UID |
| I1b | 操作性/UX | プレイ中の一時停止ができない（中断＝終了のみ） | 🧭UX 🔍QA |
| I2 | UI/ビジュアル | 開始カウントダウンの合図が弱い（小さな文字のみ） | 🎨AD 🧭UX |
| I3 | 色使い/視認性 | 防衛ライン（危険域）が薄く、どこを越えると被弾するか分かりにくい | 🎨AD 🔍QA |
| I4 | 読み込み/性能 | 非表示画面でも Canvas を毎フレーム描画し続け、無駄な負荷がある | ⚙️FE |
| I5 | UI/UX/UID | カード候補のカテゴリが文字のみで、瞬時の意思決定がしづらい | 🧭UX 🎨AD ♿UID |
| I6 | 色使い/UX | 拠点瀕死時の警告フィードバックがない | 🎨AD 🧭UX 🔍QA |
| I7 | UID/読み込み | モバイル統合（テーマカラー・ステータスバー）と入力支援、音声アンロックが未整備 | ♿UID ⚙️FE |

---

## 4. 役割ごとの意見擦り合わせと最適案の選定

各課題について役割間の意見を出し合い、「効果が高く・既存の戦闘座標やゲーム仕様（敵 HP5 倍・武器火力・出現・スコア・ランキング・80/120/120 上限・1 ファイル構成）を壊さない」を判断軸に最適案を 1 つ選ぶ。

### I1a ミュート手段
- 🧭UX: HUD に入れたいが iPhone SE では列が窮屈。
- 🎨AD: 戦闘画面の世界観を崩さない控えめなアイコンが良い。
- ⚙️FE: `beep()` 入口で `state.muted` を見るだけなら 1 行で安全。
- **採用案**: Canvas 右上に半透明アイコンボタンを重ねる（HUD グリッドを崩さない）。`localStorage` に保持。

### I1b 一時停止
- 🔍QA: 中断手段が「終了」だけなのは不便。
- ⚙️FE: ループは `dt` を 33ms 上限でクランプ済みなので、`update()` を停止しても再開時の飛びは起きない。スコアは波数基準で `elapsed` 非依存のため破綻しない。
- **採用案**: 同じ Canvas 右上にポーズボタンを追加。`state.paused` で `update()` を停止、`draw()` で「一時停止」オーバーレイ表示。波開始時に解除。

### I2 開始カウントダウン
- 🎨AD: 中央に大きな数字を出して開始の高揚感を作る。
- 🧭UX: 既存のステータス文言は残しつつ、Canvas 中央へ大きく重ねる。
- **採用案**: READY 中に Canvas 中央へ大きな「3/2/1」と「まもなく開始」をオーバーレイ描画（ロジック非干渉の描画のみ）。

### I3 防衛ライン視認性
- 🔍QA: 越えると被弾する境界が分かりづらく理不尽に感じる。
- 🎨AD: ラインを濃くしつつ、上側に淡いグラデーションの危険帯を足す。
- ⚙️FE: 既存の `layout.defenseLineY` をそのまま使い、描画だけ変更（当たり判定は不変）。
- **採用案**: `defenseLineY` 基準で淡い危険帯グラデーション＋やや濃いラインを描画。判定座標は変更しない。

### I4 非表示時の描画
- ⚙️FE: `draw()` 冒頭で PLAYING/CHOOSING/READY 以外は即 return。メニュー/結果中の無駄な 60fps 描画をなくす。
- **採用案**: `draw()` にゲーム画面状態ガードを追加。

### I5 カードのカテゴリ可読性
- 🧭UX: 8 カテゴリを色で即判別したい。
- 🎨AD: カード左端にカテゴリ色のストライプを足す（既存の金枠は維持）。
- ♿UID: 押下時の触覚的フィードバック（軽い縮小）も付ける。
- **採用案**: カテゴリ別カラーの左ボーダー＋ `:active` で軽い押下演出。

### I6 拠点瀕死フィードバック
- 🎨AD: 画面端に赤いパルス、HUD の拠点表示を赤く点滅。
- 🧭UX: しきい値は残 6 以下（約 30%）。
- **採用案**: HUD 拠点表示に瀕死クラスを付与＋ Canvas 外周に赤パルスを描画。

### I7 モバイル統合・入力支援・音声アンロック
- ♿UID: `theme-color` / iOS web-app メタ、名前入力に `enterkeyhint`、アイコンボタンに `aria-label`。
- ⚙️FE: iOS の AudioContext を開始ジェスチャ（名前入力→開始）で resume してアンロック。
- **採用案**: head にメタ追加、入力属性追加、`ensureAudio()` を開始時に呼ぶ。

---

## 5. 実装計画（index.html への適用手順）

1. **head メタ追加**: `theme-color`, `apple-mobile-web-app-capable`, `status-bar-style`, `description`。
2. **CSS 追加**（`</style>` 直前）: `.fxctl` / `.iconbtn`（Canvas 右上コントロール）、`.hud span.low` と `@keyframes hpPulse`、`.card` の押下演出。
3. **マークアップ**: `.canvas-shell` 内にミュート/ポーズの `.fxctl` を追加。名前入力に `enterkeyhint`/`inputmode`/`aria-label`。
4. **JS state**: `muted` / `paused` / `readyCount` を追加。
5. **カテゴリ色定義 `CAT_COLORS`** を追加し、`renderChoices()` でカード左ボーダーに反映。
6. **`beep()`**: `state.muted` 時は無音。**`ensureAudio()`** を追加し開始ジェスチャで resume。
7. **`update()`**: `state.paused` で停止。
8. **`startReady()` / `startWave()`**: `readyCount` 設定とポーズ解除。
9. **`draw()`**: 非ゲーム状態の描画ガード、防衛ライン強調、`drawOverlays()`（カウントダウン/一時停止/瀕死パルス）呼び出し。
10. **`updateHud()`**: 拠点瀕死クラスのトグル。
11. **イベント配線**: ミュート/ポーズボタン、`localStorage` 連携、アイコン初期化。

### 維持する仕様（変更しない）
`index.html` 1 ファイル構成、ゲーム名・`game_slug=uchikaeru`、公開 URL、60 波・40 武器・3 枚候補、敵 80/弾 120/エフェクト 120 上限、敵 HP5 倍、武器火力、敵出現、スコア、`rankingScore = clearWave * 100000000 + battleScore`、`submit_score`/`get_best_score_ranking`、Publishable key のみ、`public.scores` 不使用、戦闘座標（`getBattleLayout`/`getWeaponSlotPosition`）。

### 動作確認観点（実装後）
- 全導線が従来どおり遷移する。
- ミュートで音が止まり、再読込後も保持される。
- 一時停止中は敵・弾・スコアが止まり、再開で飛ばない。
- カウントダウン・瀕死パルス・カテゴリ色が表示される。
- iPhone SE 幅で横スクロールせず、追加ボタンが押せる。
- `node --check` でスクリプトに構文エラーがない。

---

## 6. 追加調査: 可視ラインと判定ラインのズレ

### 調査
- 🔍QA: 「敵がラインの手前で消える／被弾するように見える」との指摘を再現調査。
- ⚙️FE: コードを追跡した結果、判定（`updateEnemies`）と描画（`draw`）はどちらも同一の `layout.defenseLineY` を参照しており、**ライン座標自体は一致**していた。
- 🎨AD/⚙️FE: 真因は敵の描画。色付きの当たり判定円（中心 `e.y`・半径 `e.r`）の底は `e.y+e.r` で判定と一致するが、カエル絵文字を `e.y+e.r*0.52`・`textBaseline` 既定（alphabetic）で描いていたため、**見た目のカエルが判定円の底より約 `0.33*e.r` 上**に表示されていた（小型で約4px、ボスで約11px）。結果として「判定ラインが可視ラインより上にある」ように知覚されていた。

### 役割擦り合わせと採用案
- 案A（採用）: カエル絵文字を `textBaseline='bottom'` で当たり判定円の底 `e.y+e.r` に下端揃え。見た目の底＝本体の底＝判定＝赤い線が一致。**判定式は不変**で難易度・出現・スコアに影響なし。プラットフォーム依存のマジック数（0.52）も排除。
- 案B（不採用）: 判定を絵文字中心基準へ戻す → 「中心ではなく接触で判定」という既存方針に反し、絵文字メトリクス依存で不確実。
- 案C（不採用）: 線を絵文字中心まで上へ移動 → 拠点レイアウトが崩れる。

### 実装（第1段階）
- `textBaseline='bottom'` で `e.y+e.r` に下端揃え。
- 赤い判定ラインのコントラストを微増（`.55`→`.72`）してライン位置を明確化。

### 実装（第2段階・最終）— 残差ズレの解消
- 第1段階は em ボックス底基準のため、絵文字の下余白分の残差が残った（モデル試算: 小型 約1.6px / ボス 約4.2px）。これが動画で見えていたズレ。
- `frogInkDescent(fs)` を追加し、`ctx.measureText('🐸').actualBoundingBoxDescent`（実描画グリフの下端距離）を実測・キャッシュ。
- カエル絵文字を alphabetic ベースラインで `baseline = e.y+e.r - frogInkDescent(efs)` に描画 → グリフ実下端 = `baseline + descent` = `e.y+e.r`（＝赤い線）に一致。`measureText` と `fillText` は同一仕様の descent を用いるため、端末・フォント差によらずズレ 0。
- `textBaseline` は既定（alphabetic）のまま（第1段階の `bottom` 設定は撤去）。城・武器スロット・弾・エフェクト・オーバーレイへの影響なし。
- 当たり判定 `e.y+e.r>=defenseLineY` は不変（難易度・出現・スコアに影響なし）。

