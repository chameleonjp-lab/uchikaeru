# うちかえる

「うちかえる」は、迫るカエルの大群を拠点の自動攻撃で撃退し、ランダムに提示される武器・強化カードを選びながら 60 波クリアを目指すスマホ向けブラウザ防衛ゲームです。

公開 URL: https://chameleonjp.codeberg.page/uchikaeru/

## 現在の正仕様

- `index.html` 1 ファイル構成で、ビルド工程や複数ファイル化は行いません。
- `game_slug` は `uchikaeru`、公開 URL は `https://chameleonjp.codeberg.page/uchikaeru/` です。
- ランキング基数は `RANK_BASE = 1000000` です。送信値は `rankingScore = clearWave * 1000000 + min(999999, battleScore)` で計算し、画面表示は `○波クリア / ○○点` に分解します。
- 60 波クリア時に `battleScore` が `999,999` を超える場合、ランキング送信値では `999,999` に丸めます。これは `submit_score` 側の `score is too large` を避けるための仕様です。60 波クリア者の細かい補助点差をさらに反映したい場合は、将来の別タスクで圧縮スコア式を検討します。
- カエルの実体力は `FROG_HP_MULTIPLIER = 3.5` を現在値とします。過去調整案では 5 倍の記述がありましたが、現行コードでは 3.5 を採用しています。
- 支援・強化系は通常武器候補から除外し、武器枠を消費しない強化カードとして扱います。強化カードは `selectedWeapons` に入れず、拠点修理、全体火力強化、攻撃速度強化、敵の移動低下の各効果だけを反映します。
- 罠系は攻撃・罠武器として武器枠を消費しますが、通常弾は撃たず、防衛ライン付近に罠を設置して敵にダメージや遅延を与えます。
- 結果画面では自動で 1 回だけ `submit_score` を呼び、ランキング TOP10 と「ホームに戻る」を表示します。ランキング登録ボタンは追加しません。
- リタイア時も `submit_score` を呼び、0 波リタイアでも `play_count` に計測される仕様です。
- Supabase は Publishable key のみを使い、service_role key や `public.scores` は使いません。送信 RPC は `submit_score`、ランキング取得 RPC は `get_best_score_ranking` です。

## リポジトリ構成

- `index.html`: HTML / CSS / JavaScript を同梱したゲーム本体。ビルドなしでそのまま配信します。
- `README.md`: 仕様、ランキング連携、修正履歴を記録するドキュメントです。

npm、Vite、外部ビルド、複数ファイル化は使わず、`index.html` 1 ファイル構成を維持します。

## ゲーム仕様

- ゲーム名: うちかえる
- `game_slug`: `uchikaeru`
- テーマ: 画面上側から迫るカエルの大群を、画面下側の拠点から自動攻撃で撃退します。
- 目標: 全 60 波クリア。
- 武器: 攻撃・罠武器 7 カテゴリ × 5 種類を通常候補に出します。支援・強化系 5 種類は内部データに残っていますが、通常武器候補からは除外します。
- カード選択: 各波クリア後に 3 枚の候補から 1 つ選択します。攻撃・罠武器の追加/強化、または武器枠を消費しない強化カードが提示されます。
- 初回武器: 名前入力後の 3、2、1 カウントダウン後、1 波目の前に攻撃可能な武器だけを 3 枚提示し、1 つ選んでから 1 波を開始します。
- 敗北条件: 拠点体力が 0 になると結果画面へ進みます。
- リタイア: プレイ中にリタイアした場合も、クリア済み波数とスコアで結果を確定します。
- 性能上限: 敵 80 体、弾 120 発、エフェクト 120 個。攻撃・罠武器の最大枠は 6 個です。

## ランキング仕様

ランキングは Supabase の共通 RPC を使うベストスコア型・降順です。結果画面にランキング登録ボタンは置かず、RESULT 確定後に自動で 1 回だけ送信します。

送信値は波数を最優先するため、次の整数を使います。

```text
rankingScore = clearWave * 1000000 + min(999999, battleScore)
```

Supabase `submit_score` の実測で `p_score = 390,601,917` は `score is too large` となり、`p_score = 39,601,917` と `60,999,999` は `accepted=true` で通ることを確認しました。そのため、ランキング内部スコアの基数は `1,000,000` に確定しました。`battleScore` は最大 `999,999` に丸め、さらに送信直前の `p_score` も 0 以上の PostgreSQL `integer` 範囲内に正規化します。60 波クリア時の最大送信値は `60,999,999` です。画面表示やシェア文では送信値をそのまま出さず、必ず次の形式に分解します。

```text
○波クリア / ○○点
```

0 波クリア、敗北、リタイア、60 波クリアのいずれでも同じ形式で表示します。たとえば、39 波クリアで `battleScore = 601,917` の場合は `rankingScore = 39,601,917`、表示は `39波クリア / 601,917点` です。60 波クリアで `battleScore = 2,037,256` の場合は `battleScore` を `999,999` に丸め、`rankingScore = 60,999,999`、表示は `60波クリア / 999,999点` です。リタイア時も `submit_score` を呼び、0 波リタイアでも `0波クリア / 0点` として送信して `play_count` に計測します。

## Supabase 連携仕様

- Supabase URL: `https://mlpnjgezrnhdxsxolyzj.supabase.co`
- 使用キー: Publishable key のみ。
- secret key / service_role key は使用しません。
- `public.scores` は使用しません。
- 送信 RPC: `submit_score`
- ランキング取得 RPC: `get_best_score_ranking`
- 共通仕様上の関連 RPC: `get_first_try_ranking`, `get_game_play_stats`

スコア送信では、`p_display_name`, `p_game_slug`, `p_score`, `p_client_version` を渡します。ランキング取得では `p_game_slug` と `p_limit` を渡します。送信や取得に失敗しても結果画面とシェア機能は壊さず、無限再送もしません。結果画面にはベストランキング上位 10 件を表示し、取得できない場合はエラーメッセージ、0 件の場合は未記録メッセージを表示します。

## 今回の修正内容

- Supabase `submit_score` の独自上限に収めるため、ランキング内部スコアの基数を `1,000,000` に確定しました。
- `battleScore` は最大 `999,999` に丸め、60 波クリア時の最大送信値が `60,999,999` になるようにしました。
- 結果画面にホームへ戻るボタンを追加しました。
- 結果画面にベストランキング上位 10 件を表示するエリアを追加しました。
- リタイア時も `submit_score` を呼び、プレイ回数に計測されるようにしました。
- ランキング送信・取得に失敗しても、結果画面とシェア機能は壊さないようにしました。

- カエルの実体力は `FROG_HP_MULTIPLIER = 3.5` を現在値とします。過去調整案では 5 倍の記述がありましたが、現行コードでは 3.5 を採用しています。`ENEMY_DEFS` の個別 HP、score、damage、speed、ランキング仕様、拠点体力、武器火力、敵 80 体・弾 120 発・エフェクト 120 個上限は維持しています。
- `.screen` と `.game-wrap` の表示制御を整理し、`HOME` / `RULE` / `NAME` / `READY` / `PLAYING` / `CHOOSING` / `RESULT` / `ERROR` のうち 1 画面だけが active になるようにしました。`RESULT` 遷移時はスクロール位置も先頭に戻します。
- `.app` と `#game` を `100dvh` 内に収め、HUD、Canvas、状態表示、武器チップ、カード候補がスマホ 1 画面内で収まるように高さ設計を見直しました。カード選択欄は内部スクロールにし、結果画面がゲーム画面下に続かない構成です。
- 攻撃起点を画面中央下の 1 点から、拠点周辺に並ぶ最大 6 個の武器スロットへ変更しました。Canvas 上に台座、絵文字、Lv を描画し、弾は各武器スロットから発射されます。
- 敵の出現を横一列・斜め列・左右 2 列・ボス護衛の小隊フォーメーションに変更しました。31 波以降は同時出現数、45 波以降は後続の見え方、55 波以降はボス＋雑魚小隊の同時圧が強くなるように調整しました。
- 武器カテゴリごとに狙いを分け、連射は近距離、貫通は同レーン奥、範囲爆発と連鎖は密集、遅延は速い敵や前線、継続ダメージは高 HP を優先しつつ、同じ 1 体へ集中しすぎないよう既存弾のターゲットを加味します。
- 罠系は通常弾ではなく、防衛ライン付近に最大 20 個まで設置され、近づいた敵にダメージと遅延を与える簡易設置型です。カード説明にも「弾は撃たず、防衛ライン付近に罠を設置します」と明記しています。
- 敵本体は常にカエル絵文字として描画し、遅延や継続ダメージは輪・色・短いエフェクトで表すようにしました。範囲、連鎖、罠、支援もカテゴリごとに最低限違うエフェクトを出します。
- 下部の武器チップは長い武器名ではなく `絵文字 Lv` 表示にして、スマホ幅でも現在の構成を読みやすくしました。支援・強化カードは武器チップや武器スロットには表示しません。
- 60 波クリア、拠点突破、リタイアの直前に状態文を更新し、結果画面へ移る前の状態と結果が矛盾しにくいようにしました。
- `DEBUG=true` のときだけ、ランキング送信失敗時に RPC 名、HTTP ステータス、エラー本文先頭、`game_slug`、送信 score、名前有無を console と debug 表示に出すようにしました。`DEBUG=false` では従来通り短いユーザー向け文言だけです。
- 維持した仕様: `index.html` 1 ファイル構成、ゲーム名「うちかえる」、`game_slug=uchikaeru`、公開 URL、60 波クリア目標、3 枚候補、敵 80 体・弾 120 発・エフェクト 120 個上限、RESULT 後のランキング自動 1 回送信、ランキング登録ボタンなし、`submit_score` / `get_best_score_ranking`、`rankingScore = clearWave * 1000000 + min(999999, battleScore)`、Publishable key のみ、`public.scores` 不使用。

- 拠点ダメージ判定ラインを、画面に見えている赤い防衛ラインと同じ `layout.defenseLineY` に統一しました。
- 拠点ダメージ文字は赤い線のすぐ上に出るよう修正しました。
- 拠点ダメージ文字が上方向へ流れすぎて、画面中央でダメージが入ったように見える問題を調整しました。
- 波クリアやカード選択時に、古い拠点ダメージ文字が画面中央に残らないよう整理しました。
- 当時はダメージ判定ラインと表示位置のみを修正し、敵HP5倍、武器火力、敵出現、スコア、ランキング仕様は変更していません。

- Canvas内の主要位置を `getBattleLayout(cw, ch)` に集約し、城、防衛ライン、武器スロット、罠帯、拠点HPバーの基準を統一しました。防衛ラインと武器スロットの基準は画面上の拠点位置に合わせています。
- 武器スロットの基準を防衛ライン基準から城基準へ変更し、発射地点が上に離れすぎる問題を修正しました。
- `getWeaponSlotPosition()` を扇形の大きな上方向配置から、城の近くに並べる固定オフセット方式へ変更しました。
- 拠点ダメージ表示を城または防衛ライン近くに出るように修正しました。
- 敵の侵入判定は、カエルの中心ではなく見た目に近い接触位置で判定するよう調整しました。
- 当時は座標修正のみを行い、敵HP5倍、武器火力、敵出現、スコア、ランキング仕様は変更していません。

## 動作確認観点

- ホーム、ルール、名前入力、カウントダウン、初回武器選択、プレイ、波クリア後カード選択、結果画面へ進めること。
- 名前未入力では開始できないこと。
- 候補選択中と RESULT 後に敵・弾・当たり判定・スコア加算が止まること。
- `requestAnimationFrame` が二重起動せず、ループが止まらないこと。
- iPhone SE 幅で横スクロールせず、Canvas とカードが重ならず、下部ボタンが押せること。
- ランキング表示が `○波クリア / ○○点` 形式になること。
- 支援・強化系が通常武器候補に出ず、選んでも武器枠を消費しないこと。
- 罠系カードに、弾ではなく罠を設置する説明が表示されること。
- 60 波クリアで補助点が `999,999` に丸められる仕様と README が矛盾しないこと。
