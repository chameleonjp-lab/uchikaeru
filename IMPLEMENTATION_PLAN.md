# うちかえる UI/UX/読み込み 改善 実装計画書

- 対象: `index.html`（1ファイル構成・外部依存なしを維持）
- 作成日: 2026-06-21
- 評価軸: **ページ内デザイン / 操作性 / 読み込み処理 / UI / UX**（※ゲームバランスは対象外。既存 `PLAYTEST_REPORT.md` が別途カバー）

## 1. 進め方（役割分担とテスト）

Opus のみで 3 役に分担し、`index.html` を読み取り評価（実機操作不可のためコード静的解析＋仕様照合）。

| 役割 | 担当軸 | 指摘数 |
|------|--------|:---:|
| 🎨 UI／ビジュアルデザイナー | デザイン・UI（配色/タイポ/階層/コンポーネント/Canvas見せ方） | 12 |
| 🧭 UX／インタラクションデザイナー | UX・操作性（遷移/タップ/フィードバック/分かりやすさ/a11y） | 12 |
| ⚙️ フロントエンド／パフォーマンスエンジニア | 読み込み処理・描画性能・堅牢性 | 10 |

各役の「最重要 TOP3」:
- UI: ①敵8種が同一🐸＋近接色で識別不能 ②強敵のクラス輪郭が塗り色と同色で機能せず ③CTA優先度/ブランドの顔
- UX: ①名前入力（Enter/フォーカス/空欄フィードバック）②カード説明を「選べる情報」に ③リタイア誤タップ＋カウントダウン演出
- Perf: ①fetch タイムアウト不在で結果が「送信中」固着 ②loop の try/catch 不在で1例外が永久停止 ③targetEnemy の毎フレーム O(n²)

## 2. 役割横断の擦り合わせ（重複・対立の解決）

| 論点 | UI の意見 | UX の意見 | Perf の意見 | 擦り合わせ後の最適解 |
|------|-----------|-----------|-------------|----------------------|
| リタイアボタン | HUD から分離/アウトライン化（#6） | 誤タップ防止＋タップ領域44px↑＋確認文言改善（#4） | — | **採用**: HUDから分離・控えめ化・44px確保・確認文言を「今のスコアで終了」と結果明示 |
| カード表現 | レアフラグが描画未反映（#5） | 説明が抽象的で選べない（#2） | — | **採用（統合）**: 既存データから特徴ヒントを生成＋レア候補を視覚化（発光枠/メタ色分け） |
| 敵の識別性 | 絵文字差し替え＋色相分離（#1/#2） | 色だけ依存の状態表現（#8） | 絵文字 fillText 多発が重い（#5） | **対立解決**: README:86「常にカエル絵文字」のため**絵文字差し替えは不採用**。🐸を維持しつつ**クラス輪郭を高コントラスト（明側）化＋緑系の色相分離**で識別性を確保 |
| カウントダウン/状態文 | status の重要メッセージ強調（#12） | 3-2-1がテキストのみで弱い（#3） | AudioContext のアンロック（#8） | **採用（統合）**: Canvas中央に大きく 3/2/1 描画＋ビープ、status は重要時に強調、音はユーザー操作でアンロック |
| 結果画面 | CTA優先度設計（#4） | ボタン主従が曖昧/「もう一度」が遠回り（#5） | — | **採用（統合）**: 「もう一度」をプライマリ＋同名直行リプレイ、他はアウトライン格下げで階層化 |
| ランキング送信 | — | 失敗時の手動再試行が無い（#11） | fetch タイムアウト不在で固着（#1） | **採用（統合）**: タイムアウト＋送信/取得の並行化＋失敗時のみ「再送信」ボタン |

## 3. 採用項目（選定結果）

### A. 読み込み処理・堅牢性（最優先 / Perf 主導）
- **A1** `supabaseRpc` に `AbortController`＋8秒タイムアウト。送信(`submit_score`)と取得(`get_best_score_ranking`)を `Promise.allSettled` で並行化（「送信中」固着を解消）。
- **A2** `loop` を `try/finally` で包み、例外時も `requestAnimationFrame` を必ず再登録（1例外での永久停止を防止）。
- **A3** `visibilitychange` で PLAYING 中は自動ポーズ、復帰時に `lastTime` をリセットし dt 暴走を防止。
- **A4** ゲーム外画面（HOME/RULE/NAME/RESULT）では `draw` をスキップしアイドル時の空回しを削減。
- **A5** AudioContext をユーザー操作（開始系ボタン）で1回アンロック＋`resume()`。

### B. 描画パフォーマンス
- **B1** 背景（グリッド＋レーン＋奥行きグラデ）をオフスクリーン Canvas に1回描画→毎フレーム `drawImage`（UI#8の奥行きも同時実現、フォールバック付き）。
- **B2** `resize`/`visualViewport` ハンドラを rAF デバウンスし `resizeCanvas` 連打を抑制。
- **B3** `targetEnemy` の近傍密集度 `near` をフレーム先頭で敵ごとに1回だけ事前計算（**値は不変＝挙動不変**で O(n²)→O(n)）。

### C. UX・操作性
- **C1** 名前入力: オートフォーカス／Enter送信／空欄時のエラー枠＋メッセージ／`enterkeyhint="done"`。
- **C2** カード: 武器の特徴ヒントを説明へ追加＋レア候補を視覚化。
- **C3** カウントダウン: Canvas中央に 3/2/1 描画＋ビープ、Wave 表示を動的中心に。
- **C4** リタイア誤タップ対策（HUDから分離・44px確保・確認文言改善）。
- **C5** 結果画面の階層化＋「もう一度」同名直行リプレイ。
- **C6** a11y: `statusText` に `aria-live`、`changeState` で主見出し/主ボタンへフォーカス、`speedBtn` に `aria-pressed`/`aria-label`。
- **C7** `user-scalable=no` を撤廃（ズーム許可、WCAG 1.4.4）。
- **C8** シェア: クリップボードコピー成功/失敗のトースト表示。
- **C9** ランキング送信失敗時のみ「再送信」ボタン（多重送信防止つき）。

### D. UI・ビジュアル
- **D1** 敵識別性: クラス輪郭を高コントラスト（明側）化＋緑系の色相分離（tank を黄系から鉛色寄りへ）。🐸は維持。
- **D2** CTA階層: プライマリ強調、セカンダリをアウトライン化（1画面1緑塗り）。
- **D3** 拠点フィードバック: 被弾フラッシュ＋HP低下時の警告リング。
- **D4** ランキング: 1〜3位を金銀銅＋自分の行をハイライト。
- **D5** status の重要メッセージ強調（ボス波/クリア/敗北で色・サイズ変化）＋`clamp` 化。
- **D6** 装備チップを `flex-wrap` で最大6個表示（見切れ解消）。
- **D7** 角丸/枠線のデザイントークン正規化（`--r-sm/md/lg/pill`、`--line-weak/line/strong`）。
- **D8** タイトルのグラデ文字＋frogline 演出（Webフォント不使用）。

## 4. 見送り（理由つき）
- 敵タイプの**絵文字差し替え**: README:86「常にカエル絵文字」に抵触 → 色相/輪郭で代替。
- **Webフォント導入**: 1ファイル/外部依存なしの方針（README:9,25）に抵触 → グラデ文字で代替。
- **記録しない中断**（リタイア以外の出口）: README:17「0波リタイアも play_count に計測」と整合させる仕様調整が必要 → 別タスク。

## 5. 不変条件（壊さないもの）
- 1ファイル構成・外部依存なし・`game_slug=uchikaeru`・公開URL。
- ゲームバランス（`FROG_HP_MULTIPLIER`、`ENEMY_DEFS` の hp/speed/damage/score、武器火力、敵80/弾120/エフェクト120、`RANK_BASE`、`rankingScore` 計算式）。
- ランキング自動送信は RESULT 後1回／登録ボタンなし、`submit_score`・`get_best_score_ranking`、Publishable key のみ。

## 6. 実装順序と検証
1. HTML `head`（C7）→ 2. CSS（D系/トークン/トースト/レア/階層）→ 3. HTML body（C1/C4/C5/C6/C8/C9/D8）→ 4. JS（A/B/C/D の挙動）。
- 検証: `<script>` を抽出し `node --check` で構文検証。仕様不変条件を grep で再確認。状態遷移（HOME→…→RESULT）とイベントハンドラの結線を目視確認。
